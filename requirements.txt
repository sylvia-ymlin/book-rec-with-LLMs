# =============================================================================
# Book Recommendation System — Dependencies
# =============================================================================
#
# LAYERED INSTALL:
#   Production (API + RAG + RecSys):  pip install -r requirements-base.txt
#   Development (+ tests, ruff):      pip install -r requirements-dev.txt
#   Fine-tuning (LoRA/SFT scripts):   pip install -r requirements-optional.txt
#
# LOCK FILE (reproducible builds via pip-tools):
#   pip install pip-tools
#   pip-compile requirements-base.txt -o requirements-base.lock
#   pip-sync requirements-base.lock
#
# LEGACY: Full install (base + dev, no optional):
#   pip install -r requirements-base.txt -r requirements-dev.txt
# =============================================================================

-r requirements-base.txt
-r requirements-dev.txt
