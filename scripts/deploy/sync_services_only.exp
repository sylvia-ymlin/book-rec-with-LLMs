#!/usr/bin/expect

set host "connect.nmb2.seetacloud.com"
set port "15054"
set user "root"
set password "9Dml+WZeqp5b"
set local_dir "."
set remote_dir "/root/autodl-tmp/book-rec-with-LLMs"

# Force Create output dir
spawn ssh -p $port $user@$host "mkdir -p $remote_dir/src/services"
expect {
    "password:" { send "$password\r" }
    "continue connecting" { send "yes\r"; exp_continue }
}
expect eof

# Sync services
spawn scp -P $port -r $local_dir/src/services $user@$host:$remote_dir/src/
expect {
    "password:" { send "$password\r" }
}
expect eof

puts "Services Sync Complete!"
